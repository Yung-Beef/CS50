#include <cs50.h>
#include <stdio.h>

int main(void)
{
    // Gets the size of the grid as a positive integer
    int n = get_size();

    // Prints a grid of bricks
    print_grid(n);
}

int get_size(void)
{
    
}

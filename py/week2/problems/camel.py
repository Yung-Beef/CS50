input = input("camelCase: ")
first = None
second = None

for _ in input:
    while isupper(input[_]) == False:
        first = first + input[_]
    if isupper(input[_])
        second = second + input[_]
        
        i = 1
        while i < _:
            first = first + input[_]
        i = 0
        while i < (len(input) - _):
            second = second +
